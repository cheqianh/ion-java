name: Greetings

on: [pull_request, issues]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: pwd
      - name: download
        run: echo $GITHUB_WORKSPACE
      - name: Checkout
        uses: actions/checkout@master
      - id: ls
        run: ls